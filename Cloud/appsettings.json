{
  "Logging": {
    "LogLevel": {
      "Default": "Information",
      "Microsoft.AspNetCore": "Warning"
    }
  },
  "AllowedHosts": "*",
  "Port": "7777",
  "CloudPath": "", // Indicate the path for the cloud files if different from AppData
  "EntryPoint": "test.cloudservices.agency" // Used if not specified in the QR code. Server entry point address (for release use: server.cloudservices.agency)
}
