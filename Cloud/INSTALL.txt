Installation is very simple!

========= MacOS:

From terminal run the command ./install.sh

========= LINUX:

From terminal run the command ./install.sh

========= WINDOWS:

Unpack the package in the directory where you want to run the program, for example
C:\Program Files\Cloud
Then run Cloud.exe (if prompted, also install any frameworks)